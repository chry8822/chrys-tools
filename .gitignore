# 의존성
node_modules/

# 빌드 결과물
dist/

# npm pack 생성 파일
*.tgz

# 환경변수
.env

# 로컬 작업 메모 (개인 작업 기록, 커밋 불필요)
WORK_LOG.md

# 커밋 메시지 임시 파일 (PowerShell 커밋 플로우에서 자동 생성·삭제)
COMMIT_MSG_TEMP.txt

# Cursor/Claude 로컬 설정 (기기마다 다른 개인 설정)
.claude/settings.local.json

# 블로그 포스트 자동 생성 결과물
blog-output/

# 개인 아이디어 메모 (커밋 불필요)
MOBILE_NOTIFY_IDEA.md

# 발표용 HTML 작업 파일 (배포는 docs/index.html로 진행)
presentation*.html
